# venv / caches
.venv/
__pycache__/
**/__pycache__/
.pytest_cache/
.ruff_cache/
*.pyc

# git / CI / tooling
.git/
.github/
.pre-commit-config.yaml
.gitignore

# local env / dev-only
.env
requirements-dev.txt
pyproject.toml

# runtime에 안 쓰는 폴더(배포 방식에 따라 조정)
data/
models/
preprocess_data/
recommend/
tests/
loadtest/
db_tuning/
docs/
infra/
k8s/
ops/

# CI / repo-level files not needed in runtime image build context
Jenkinsfile
bentofile.yaml
docker-compose.yaml
docker-compose.prod.yaml
init.sh

# scripts 중 런타임 이미지에 사용하지 않는 항목
scripts/ci/
