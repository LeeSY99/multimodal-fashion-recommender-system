apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - pvc-data.yaml
  - pvc-models.yaml
  - job-sync-assets.yaml

patches:
  - path: patch-deployment.yaml
  - path: patch-ingress.yaml
